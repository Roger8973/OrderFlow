-- Baseline migration. Establishes the DbUp-managed migration pipeline;
-- DbUp creates and maintains its own SchemaVersions tracking table.
-- No application schema exists yet; future module migrations start from here.
SELECT 1;
